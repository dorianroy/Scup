package com.dasflash.soundcloud.scup.view.skins
{
	
	/*
		WORKAROUND: couldn't use borderSides with the default HaloBorder class
		but every other skin seems work - so the purpose of this class is just
		to be something else than HaloBorder
		Maybe a future spark Grid component will fix this
	*/
	import mx.skins.halo.HaloBorder;

	public class GridRowBorderSkin extends HaloBorder
	{
	}

}